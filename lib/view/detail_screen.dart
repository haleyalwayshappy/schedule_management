import 'package:flutter/material.dart';

// TODO  : 상세 페이지
//  추후에 add page와 합침 가능성 고려
class DetailScreen extends StatelessWidget {
  const DetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("디테일 페이지다!"),
    );
  }
}
